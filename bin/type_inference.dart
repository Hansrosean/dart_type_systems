void main() {
  // variabel map dengan tipe eksplisit
  Map<String, dynamic> dataProfile = {'name': 'Hans', 'age': 21};

  // variabel map dengan tipe implisit
  var dataProfile2 = {'name': 'Hans', 'age': 21};
}
